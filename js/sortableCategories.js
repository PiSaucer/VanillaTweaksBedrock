new Sortable(aesthetic, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Terrain, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Lower, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Peace, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Utility, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Unobtrusive, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(GUIDiv, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Fun, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Custom, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});

new Sortable(Panoramas, {
    animation: 150,
    group: 'shared',
    ghostClass: 'sortable-ghost'
});